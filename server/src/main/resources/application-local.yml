spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:h2:mem:test


  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379
    password: wls5177


logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG


jwt.key: stackOverflow
jwt:
  access-token-expiration-minutes: 60 * 60 * 1000
  refresh-token-expiration-minutes: 4 * 60 * 60 * 1000
